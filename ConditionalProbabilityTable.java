import java.util.Map;

public class ConditionalProbabilityTable {
	
	HashMap<RandomVariable, Integer> encoding;
/*	double[][] values;
	
	public ConditionalProbabilityTable(RandomVariable variable, List<RandomVariable> givens) {
		encoding = new HashMap<RandomVariable, Integer>();
		for (int i=0; i<givens.size(); i++) {
			encoding.put(givens.get(i-1), Integer.parse(i));
		}
		encoding.put(variables, 0);
		
		values = new double[(int)Math.pow(2, givens.size())][givens.size()+2];
		fillValues(variable, givens, 0);
	} 
*/
	
}